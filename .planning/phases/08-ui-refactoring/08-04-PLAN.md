---
phase: 08-ui-refactoring
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["ui-react/src/components/widgets/HeroMetric.tsx", "ui-react/src/components/widgets/KeyValueGrid.tsx", "ui-react/src/components/widgets/ListWidget.tsx"]
autonomous: true
requirements: ["REQ-UI-REF-003"]
must_haves:
  truths:
    - "Widgets use tabular-nums for numeric rendering"
    - "Long values are truncated gracefully with ellipsis and use tooltips"
    - "Widgets exhibit a brief background color flash on data updates"
  artifacts:
    - path: "ui-react/src/components/widgets/HeroMetric.tsx"
      provides: "High-density hero metric UI"
    - path: "ui-react/src/components/widgets/KeyValueGrid.tsx"
      provides: "High-density grid UI"
    - path: "ui-react/src/components/widgets/ListWidget.tsx"
      provides: "High-density list UI"
---

<objective>
Refactor remaining Data Widgets.

Purpose: To apply high-density formatting, tabular numbers, truncation handling, and data update animations to the remaining data presentation widgets.
Output: Modernized `HeroMetric.tsx`, `KeyValueGrid.tsx`, and `ListWidget.tsx`.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refactoring/08-CONTEXT.md
@.planning/phases/08-ui-refactoring/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor HeroMetric</name>
  <files>ui-react/src/components/widgets/HeroMetric.tsx</files>
  <action>
    - Apply `tabular-nums` to large numbers.
    - Implement truncation (`truncate`) and wrap full text in `<Tooltip>` for long values.
    - Add a brief background color flash animation triggered when the underlying data changes.
  </action>
  <verify>
    <automated>grep -q "tabular-nums" ui-react/src/components/widgets/HeroMetric.tsx</automated>
  </verify>
  <done>HeroMetric renders perfectly with tabular numbers and data update animations.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor KeyValueGrid</name>
  <files>ui-react/src/components/widgets/KeyValueGrid.tsx</files>
  <action>
    - Apply `tabular-nums` where numbers are presented.
    - Implement truncation and tooltips for both keys and values to preserve layout integrity.
    - Add data update flash animation on value changes.
  </action>
  <verify>
    <automated>grep -q "truncate" ui-react/src/components/widgets/KeyValueGrid.tsx</automated>
  </verify>
  <done>KeyValueGrid handles high-density text beautifully without layout shifts.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor ListWidget</name>
  <files>ui-react/src/components/widgets/ListWidget.tsx</files>
  <action>
    - Tighten row padding and font sizes for high information density.
    - Apply truncation and tooltips for long list items.
    - Ensure list scrollbars (if any) are minimal and match the design system.
  </action>
  <verify>
    <automated>grep -q "truncate" ui-react/src/components/widgets/ListWidget.tsx</automated>
  </verify>
  <done>ListWidget displays data compactly and degrades gracefully on overflow.</done>
</task>

</tasks>

<verification>
Run `npm run build` in `ui-react`.
</verification>

<success_criteria>
All data widgets follow the high-density constraints, gracefully handle overflow, and visually indicate updates.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refactoring/08-04-SUMMARY.md`
</output>
