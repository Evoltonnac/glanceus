---
phase: quick
plan: 260318-u6b
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-react/src/pages/Dashboard.tsx
  - core/refresh_policy.py
  - core/api.py
  - tests/api/test_source_auto_refresh_api.py
  - tests/core/test_refresh_scheduler.py
autonomous: true
requirements: []
---

<objective>
Fix two interaction issues: (1) Dashboard source polling should request immediately when polling starts, and (2) integration `default_refresh_interval_minutes` should no longer silently ignore valid minute values like `120`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard polling immediate fetch + stable interval lifecycle</name>
  <files>ui-react/src/pages/Dashboard.tsx</files>
  <action>
Refactor polling effect to invoke one immediate fetch when the effect starts, then continue 3-second polling. Use a ref for latest `dataMap` to avoid stale reads and avoid effect dependency loops that can cause repeated immediate requests.
  </action>
  <verify>
    <automated>make test-frontend</automated>
  </verify>
  <done>Opening Dashboard no longer waits for first interval tick before requesting source list data.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh policy consistency for integration defaults</name>
  <files>core/refresh_policy.py, core/api.py</files>
  <action>
Keep source/global refresh interval constraints unchanged (fixed options), but allow integration `default_refresh_interval_minutes` to accept any non-negative integer so values like `120` are effective instead of silently falling back.
  </action>
  <verify>
    <automated>make test-backend</automated>
  </verify>
  <done>Integration-level default refresh intervals like 120 minutes are recognized and applied by API/scheduler.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests for integration interval behavior</name>
  <files>tests/api/test_source_auto_refresh_api.py, tests/core/test_refresh_scheduler.py</files>
  <action>
Add tests that cover `default_refresh_interval_minutes: 120` so API effective interval output and scheduler due logic both validate the new behavior.
  </action>
  <verify>
    <automated>make test-impacted</automated>
  </verify>
  <done>Regression coverage prevents future silent fallback regressions for integration custom minute values.</done>
</task>

</tasks>

<success_criteria>
- Dashboard polling performs an immediate request when polling starts
- Integration default refresh interval supports 120 minutes (and other non-negative integers)
- Source/global refresh interval option contract remains unchanged
- API list sources shows integration default 120 as effective when applicable
- Scheduler honors integration default 120 for enqueue timing
- Impacted test gate passes
</success_criteria>

<output>
After completion, create `.planning/quick/260318-u6b-dashboard-sources-default-refresh-interv/260318-u6b-SUMMARY.md`.
</output>
