---
phase: 11-tdd-test-coverage-tdd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-react/package.json
  - ui-react/package-lock.json
  - ui-react/vitest.config.ts
  - ui-react/src/test/setup.ts
  - ui-react/src/test/mocks/tauri.ts
  - ui-react/src/api/client.test.ts
  - ui-react/src/store/index.test.ts
autonomous: true
requirements:
  - "测试覆盖与 TDD 规范确立 (Test Coverage & TDD)"
must_haves:
  truths:
    - "Frontend core behavior tests run in a consistent `Vitest + React Testing Library` environment."
    - "Tauri bridge and browser APIs are mocked in one shared setup path."
    - "Frontend test and typecheck commands are explicit and reusable in CI."
  artifacts:
    - path: ui-react/vitest.config.ts
      provides: "Vitest test runner configuration with jsdom and setup wiring."
    - path: ui-react/src/test/setup.ts
      provides: "Global mocks and testing-library defaults."
    - path: ui-react/package.json
      provides: "Test scripts used by impacted checks and CI gates."
  key_links:
    - from: ui-react/vitest.config.ts
      to: ui-react/src/test/setup.ts
      via: "Single-source test environment bootstrap."
---

<objective>
Build frontend testing infrastructure before adding component/page coverage plans.

Purpose: Ensure Phase 11 frontend tests are deterministic and maintainable instead of ad-hoc local checks.
Output: Standard Vitest runtime, shared mocks, and stable frontend test commands.
</objective>

<context>
@.planning/phases/11-tdd-test-coverage-tdd/11-CONTEXT.md
@ui-react/package.json
@ui-react/vite.config.ts
@ui-react/src/store/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Frontend Test Dependencies and Scripts</name>
  <files>ui-react/package.json, ui-react/package-lock.json</files>
  <action>Add `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, and related typings/helpers. Define scripts for `test`, `test:core`, and `typecheck` so frontend quality gates have stable entry points.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run</automated>
  </verify>
  <done>Frontend test dependencies are installed and scripts execute from `ui-react/package.json`.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Shared Frontend Test Environment</name>
  <files>ui-react/vitest.config.ts, ui-react/src/test/setup.ts, ui-react/src/test/mocks/tauri.ts</files>
  <action>Configure Vitest with jsdom, alias resolution, and shared setup file. Centralize Tauri `invoke/listen` mocks and global browser/test cleanup behavior in setup utilities.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/api/client.test.ts</automated>
  </verify>
  <done>Frontend tests run with consistent mocks and without per-file boilerplate.</done>
</task>

<task type="auto">
  <name>Task 3: Add Harness Smoke Tests for API Client and Store</name>
  <files>ui-react/src/api/client.test.ts, ui-react/src/store/index.test.ts</files>
  <action>Add minimal non-snapshot tests proving fetch error handling in `api/client.ts` and state transitions in `store/index.ts`, ensuring the new harness can validate core behavior safely.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/api/client.test.ts src/store/index.test.ts</automated>
  </verify>
  <done>Frontend harness smoke tests pass and validate that test infra is ready for deeper coverage plans.</done>
</task>

</tasks>

<verification>
Run both `npm run test -- --run` and `npm run typecheck` in `ui-react` to confirm baseline gate commands are operational.
</verification>

<success_criteria>
Frontend test infrastructure is standardized and supports Phase 11 component/page test plans without rework.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tdd-test-coverage-tdd/11-02-SUMMARY.md`
</output>
