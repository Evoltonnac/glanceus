---
phase: 11-tdd-test-coverage-tdd
plan: 03
type: tdd
wave: 2
depends_on:
  - 11-01
files_modified:
  - tests/core/test_executor_auth_interactions.py
  - tests/core/test_source_state.py
  - tests/core/test_encryption.py
  - tests/api/test_auth_status.py
  - tests/helpers/mock_runtime.py
  - core/api.py
autonomous: true
requirements:
  - "测试覆盖与 TDD 规范确立 (Test Coverage & TDD)"
must_haves:
  truths:
    - "Executor interaction behavior is covered for API key, OAuth, cURL, and webview-scraper auth paths."
    - "Source state transitions and encryption helpers are protected by regression tests."
    - "High-risk auth status API behavior returns deterministic states for missing/ok/error paths."
  artifacts:
    - path: tests/core/test_executor_auth_interactions.py
      provides: "Release-blocking auth-chain behavior tests."
    - path: tests/api/test_auth_status.py
      provides: "Critical route coverage for `/sources/{id}/auth-status`."
    - path: tests/core/test_encryption.py
      provides: "Encryption safety and compatibility regression coverage."
  key_links:
    - from: core/api.py
      to: tests/api/test_auth_status.py
      via: "Route behavior and error-branch expectations."
---

<objective>
Deliver backend core release-blocking coverage using strict red-green TDD loops.

Purpose: Protect the highest-risk backend behavior before Phase 13 release hardening.
Output: Backend core test suite for auth interactions, state machine behavior, encryption, and auth status API route.
</objective>

<context>
@.planning/phases/11-tdd-test-coverage-tdd/11-CONTEXT.md
@core/executor.py
@core/source_state.py
@core/encryption.py
@core/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Auth Interaction Matrix Tests for Executor</name>
  <files>tests/core/test_executor_auth_interactions.py, tests/helpers/mock_runtime.py</files>
  <action>Write failing tests first for missing-auth interaction generation across `api_key`, `oauth`, `curl`, and `webview` flow steps, then implement/fix runtime behavior until all scenarios produce expected `InteractionType` and `SourceStatus` outputs.</action>
  <verify>
    <automated>python -m pytest tests/core/test_executor_auth_interactions.py -q</automated>
  </verify>
  <done>All four required auth chains are explicitly covered and validated as release-blocking behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add Source State and Encryption Regression Tests</name>
  <files>tests/core/test_source_state.py, tests/core/test_encryption.py</files>
  <action>Create unit tests for SourceState defaults/enum transitions and encryption roundtrip/error behavior (`ENC:` prefixed and plain values). Use TDD flow for each behavior branch before any production change.</action>
  <verify>
    <automated>python -m pytest tests/core/test_source_state.py tests/core/test_encryption.py -q</automated>
  </verify>
  <done>State and encryption utilities have repeatable regression protection against accidental behavior drift.</done>
</task>

<task type="auto">
  <name>Task 3: Cover and Stabilize Auth Status API Route</name>
  <files>tests/api/test_auth_status.py, core/api.py</files>
  <action>Add API tests for `/api/sources/{source_id}/auth-status` across not-found/error/missing/ok branches, then patch route logic as needed (including undefined variable and auth branch edge cases) so behavior is deterministic.</action>
  <verify>
    <automated>python -m pytest tests/api/test_auth_status.py -q</automated>
  </verify>
  <done>Auth status endpoint behavior is fully covered for critical branches and no longer relies on implicit runtime assumptions.</done>
</task>

</tasks>

<verification>
Execute backend core suite subset and confirm no skipped release-blocking scenarios for auth chains and critical route behavior.
</verification>

<success_criteria>
Backend release-blocking modules (`executor`, `source_state`, `encryption`, auth-status API) are protected by deterministic tests aligned with Phase 11 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tdd-test-coverage-tdd/11-03-SUMMARY.md`
</output>
