---
phase: 02-i18n-i18n-30-5-4-5-5-30-1
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - core/refresh_policy.py
  - core/settings_manager.py
  - core/api.py
  - main.py
  - ui-react/src/pages/Settings.tsx
  - ui-react/src/pages/Dashboard.tsx
  - tests/api/test_source_auto_refresh_api.py
  - tests/core/test_refresh_scheduler.py
  - tests/core/test_settings_manager.py
  - tests/core/test_app_startup_resilience.py
autonomous: true
requirements:
  - "P2-REQ-04"
must_haves:
  truths:
    - "Global refresh defaults to 30 minutes with practical options between 5 minutes and 1 day."
    - "Encryption is enabled by default and first startup provisions a master key once without breaking existing flows."
  artifacts:
    - path: "core/refresh_policy.py"
      provides: "Canonical refresh option set and default interval policy."
    - path: "core/settings_manager.py"
      provides: "Default-on encryption settings and key provisioning compatibility."
  key_links:
    - from: "core/refresh_policy.py"
      to: "ui-react/src/pages/Settings.tsx"
      via: "shared refresh option semantics and default value display"
    - from: "main.py"
      to: "core/settings_manager.py"
      via: "startup path provisions key when encryption default is enabled"
---

# Phase 2 Plan 03: Refresh/Encryption Defaults and Settings UX Hardening

## Objective
- **What:** Align refresh and encryption defaults with phase targets, including startup provisioning behavior and UI option alignment.
- **Why:** Phase 2 requires safer out-of-box defaults and predictable settings behavior before release.
- **Output:** Updated refresh policy contract, default-on encryption with startup key provisioning, and regression coverage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-i18n-i18n-30-5-4-5-5-30-1/02-RESEARCH.md
@.planning/phases/02-i18n-i18n-30-5-4-5-5-30-1/02-02-PLAN.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Update refresh policy defaults and option contract end-to-end</name>
  <files>core/refresh_policy.py, core/api.py, ui-react/src/pages/Settings.tsx, ui-react/src/pages/Dashboard.tsx, tests/api/test_source_auto_refresh_api.py, tests/core/test_refresh_scheduler.py</files>
  <action>Set global default refresh to 30 minutes and enforce option set constrained to 5 minutes through 1 day (4-5 choices). Keep override precedence (`source > integration > global`) intact and update UI option rendering to match backend policy.</action>
  <verify>
    <automated>pytest tests/api/test_source_auto_refresh_api.py tests/core/test_refresh_scheduler.py -q</automated>
  </verify>
  <done>Refresh defaults and selectable options align with phase requirements without regressing existing override resolution behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make encryption default-on and provision key once on startup</name>
  <files>core/settings_manager.py, main.py, core/api.py, tests/core/test_settings_manager.py, tests/core/test_app_startup_resilience.py</files>
  <action>Change settings defaults to enable encryption by default and ensure startup path provisions master key once when missing. Preserve compatibility with existing saved settings and existing migration/import/export flows.</action>
  <verify>
    <automated>pytest tests/core/test_settings_manager.py tests/core/test_app_startup_resilience.py -q</automated>
  </verify>
  <done>New installs have encryption enabled with stable key provisioning, and existing users retain safe upgrade behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Final UX alignment for advanced settings interactions</name>
  <files>ui-react/src/pages/Settings.tsx</files>
  <action>Adjust advanced settings labels/help text and state transitions so users clearly understand refresh behavior and encryption defaults, including save feedback and key lifecycle hints.</action>
  <verify>
    <automated>make test-impacted</automated>
  </verify>
  <done>Settings UX communicates defaults clearly and interaction flows remain stable across desktop/web modes.</done>
</task>

## Verification
- Validate a fresh run path yields refresh default 30 and encryption enabled with one-time key setup.
- Validate existing user data/settings continue to load without migration breakage.

## Success Criteria
- [ ] Refresh default/options match phase requirements and remain policy-consistent across backend + UI.
- [ ] Encryption default-on path is reliable and does not regress existing startup behavior.
