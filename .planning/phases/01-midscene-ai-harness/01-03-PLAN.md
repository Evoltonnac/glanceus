---
phase: "01"
plan: "03"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "ui-react/tests/e2e/midscene/dashboard-crud.spec.ts"
  - "ui-react/tests/e2e/midscene/source-crud.spec.ts"
  - "ui-react/tests/e2e/midscene/step-failure.spec.ts"
autonomous: true
requirements:
  - "D-02"

must_haves:
  truths:
    - "AI can perform dashboard CRUD operations"
    - "AI can perform source CRUD operations"
    - "AI can rerun source sync"
    - "AI can handle step failure with rollback behavior"
  artifacts:
    - path: "ui-react/tests/e2e/midscene/dashboard-crud.spec.ts"
      provides: "D-02 dashboard CRUD tests"
      min_lines: 30
    - path: "ui-react/tests/e2e/midscene/source-crud.spec.ts"
      provides: "D-02 source CRUD and rerun tests"
      min_lines: 30
    - path: "ui-react/tests/e2e/midscene/step-failure.spec.ts"
      provides: "D-02 step failure and rollback tests"
      min_lines: 30
  key_links:
    - from: "dashboard-crud.spec.ts"
      to: "midscene.fixture.ts"
      via: "import { test } from './midscene.fixture'"
    - from: "source-crud.spec.ts"
      to: "midscene.fixture.ts"
      via: "import { test } from './midscene.fixture'"
    - from: "step-failure.spec.ts"
      to: "midscene.fixture.ts"
      via: "import { test } from './midscene.fixture'"
---

<objective>
Create D-02 additional key interaction tests: dashboard CRUD, source CRUD/rerun, step failure/rollback.
</objective>

<context>
@ui-react/tests/e2e/midscene/midscene.fixture.ts
@ui-react/tests/e2e/test_ui.spec.ts

# Test naming convention from CONVENTIONS.md:
- camelCase for test files: `dashboard-crud.spec.ts`, `source-crud.spec.ts`, `step-failure.spec.ts`
- kebab-case for config files

# Existing mocking pattern from test_ui.spec.ts:
```typescript
test.beforeEach(async ({ page }) => {
    await page.route("**/*", async (route) => {
        // Mock all API endpoints
    });
});
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard-crud.spec.ts</name>
  <files>ui-react/tests/e2e/midscene/dashboard-crud.spec.ts</files>
  <action>
Create `ui-react/tests/e2e/midscene/dashboard-crud.spec.ts` with AI-driven dashboard CRUD tests:

**Tests to implement:**
1. Create new dashboard - AI navigates to dashboard, creates new view
2. Read dashboard - AI verifies dashboard renders with expected widgets
3. Update dashboard - AI modifies dashboard layout/widgets
4. Delete dashboard - AI deletes a dashboard and verifies removal

**Implementation:**
- Import { test } from './midscene.fixture'
- Use page.route() to mock API responses for views CRUD
- Use agent.aiAct(), agent.aiWaitFor(), agent.aiAssert() for AI-driven interactions
- Each test should be independent and mock state in beforeEach

Example structure:
```typescript
import { test } from './midscene.fixture';

test.describe('Dashboard CRUD', () => {
    test.beforeEach(async ({ page }) => {
        // Mock views API
        await page.route('**/api/views**', async (route) => {
            return route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify([]) });
        });
    });

    test('create dashboard', async ({ agentForPage, page }) => {
        await page.goto('/');
        const agent = await agentForPage(page);
        await agent.aiAct('navigate to create a new dashboard');
        await agent.aiWaitFor('the dashboard creation form is visible');
        await agent.aiAssert('the dashboard can be created with a name');
    });
    // ... other CRUD tests
});
```
</action>
  <verify>
    <automated>cat /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react/tests/e2e/midscene/dashboard-crud.spec.ts | grep -E "test\(|aiAct|aiWaitFor|aiAssert"</automated>
  </verify>
  <done>dashboard-crud.spec.ts exists with create/read/update/delete tests using AI-driven interactions</done>
</task>

<task type="auto">
  <name>Task 2: Create source-crud.spec.ts</name>
  <files>ui-react/tests/e2e/midscene/source-crud.spec.ts</files>
  <action>
Create `ui-react/tests/e2e/midscene/source-crud.spec.ts` with AI-driven source CRUD and rerun tests:

**Tests to implement:**
1. Create source - AI creates new source via integration
2. Read source - AI verifies source appears in source list with correct status
3. Update source - AI modifies source configuration
4. Delete source - AI deletes a source
5. Rerun source - AI triggers source sync/refetch

**Implementation:**
- Import { test } from './midscene.fixture'
- Use page.route() to mock API responses for sources CRUD and data fetch
- Use agent.aiAct(), agent.aiWaitFor(), agent.aiAssert() for AI-driven interactions

```typescript
test.describe('Source CRUD and Rerun', () => {
    test('rerun source sync', async ({ agentForPage, page }) => {
        await page.goto('/sources');
        const agent = await agentForPage(page);
        await agent.aiAct('click the refresh button on the first source card');
        await agent.aiWaitFor('the source status updates');
        await agent.aiAssert('the source shows success or pending status');
    });
    // ... other tests
});
```
</action>
  <verify>
    <automated>cat /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react/tests/e2e/midscene/source-crud.spec.ts | grep -E "test\(|aiAct|aiWaitFor|aiAssert"</automated>
  </verify>
  <done>source-crud.spec.ts exists with CRUD and rerun tests using AI-driven interactions</done>
</task>

<task type="auto">
  <name>Task 3: Create step-failure.spec.ts</name>
  <files>ui-react/tests/e2e/midscene/step-failure.spec.ts</files>
  <action>
Create `ui-react/tests/e2e/midscene/step-failure.spec.ts` with AI-driven step failure and rollback tests:

**Tests to implement:**
1. Step failure detection - AI triggers an integration with a failing step, verifies error state
2. Step rollback - AI verifies auto-rewind to previous successful step
3. Error recovery - AI attempts to recover from step failure

**Implementation:**
- Import { test } from './midscene.fixture'
- Use page.route() to mock API responses that indicate step failure
- Use agent.aiAct(), agent.aiWaitFor(), agent.aiAssert() for AI-driven interactions

```typescript
test.describe('Step Failure and Rollback', () => {
    test('detects step failure state', async ({ agentForPage, page }) => {
        await page.goto('/integrations');
        const agent = await agentForPage(page);
        // Mock API to return error status
        await page.route('**/api/sources*', async (route) => {
            return route.fulfill({
                status: 200,
                contentType: 'application/json',
                body: JSON.stringify([{ id: 'fail-source', status: 'error', error: 'Step failed' }])
            });
        });
        await agent.aiAct('refresh the source list');
        await agent.aiWaitFor('an error indicator appears on the failed source');
        await agent.aiAssert('the error message is displayed');
    });
    // ... rollback and recovery tests
});
```
</action>
  <verify>
    <automated>cat /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react/tests/e2e/midscene/step-failure.spec.ts | grep -E "test\(|aiAct|aiWaitFor|aiAssert"</automated>
  </verify>
  <done>step-failure.spec.ts exists with failure/rollback/recovery tests using AI-driven interactions</done>
</task>

</tasks>

<verification>
cd /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react && pnpm exec playwright test tests/e2e/midscene/ --list 2>&1 | grep -E "dashboard-crud|source-crud|step-failure"
</verification>

<success_criteria>
- dashboard-crud.spec.ts exists with 4 CRUD tests (create/read/update/delete)
- source-crud.spec.ts exists with CRUD + rerun tests
- step-failure.spec.ts exists with failure detection and rollback tests
- All tests use AI-driven interactions (aiAct/aiWaitFor/aiAssert)
- All tests mock API responses via page.route()
</success_criteria>

<output>
After completion, create `.planning/phases/01-midscene-ai-harness/01-03-SUMMARY.md`
</output>
