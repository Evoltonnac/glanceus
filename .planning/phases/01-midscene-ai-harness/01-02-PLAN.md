---
phase: "01"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "ui-react/tests/e2e/midscene/critical-path.spec.ts"
autonomous: true
requirements:
  - "D-01"
  - "D-05"
  - "D-06"
  - "D-07"
  - "D-09"
  - "D-10"

must_haves:
  truths:
    - "AI can create integration via YAML config copy"
    - "AI can create source from integration"
    - "AI can switch to dashboard and complete auth/form flow"
    - "AI can add widget to dashboard"
    - "AI can verify widget renders correctly"
  artifacts:
    - path: "ui-react/tests/e2e/midscene/critical-path.spec.ts"
      provides: "D-01 critical path E2E test"
      min_lines: 50
      exports: ["critical-path suite with AI-driven steps"]
  key_links:
    - from: "ui-react/tests/e2e/midscene/critical-path.spec.ts"
      to: "midscene.fixture.ts"
      via: "import { test } from './midscene.fixture'"
    - from: "critical-path.spec.ts"
      to: "config/examples/test-integrations/"
      via: "AI reads YAML configs at test time"
---

<objective>
Create D-01 critical path E2E test: create integration -> create source -> switch to dashboard -> complete auth/form -> add widget -> render UI.
</objective>

<context>
@ui-react/tests/e2e/midscene/midscene.fixture.ts
@config/examples/integrations/github_profile_pulse.yaml

# AI-driven test pattern:
```typescript
import { test } from './midscene.fixture';

test('critical path flow', async ({ agentForPage, page }) => {
  await page.goto('/');

  // AI drives browser via natural language
  const agent = await agentForPage(page);

  // Navigate to integrations
  await agent.aiAct('click the "Integrations" link in the navigation');

  // Create integration from YAML config
  await agent.aiAct('click "New Integration" button');
  await agent.aiWaitFor('the integration dialog appears');

  // AI reads predefined YAML and creates real integration
  await agent.aiAct('select the OAuth preset');
  // ... fill form using mock OAuth via page.route()
});
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create critical-path.spec.ts</name>
  <files>ui-react/tests/e2e/midscene/critical-path.spec.ts</files>
  <action>
Create `ui-react/tests/e2e/midscene/critical-path.spec.ts` implementing the D-01 critical path E2E test:

**Test flow:**
1. Navigate to integrations page
2. Create integration via AI reading a predefined YAML config from config/examples/test-integrations/
3. Mock OAuth provider responses via page.route() (D-05)
4. Create source from the integration
5. Switch to dashboard page
6. Complete any required auth/form interactions (mocked)
7. Add a widget to the dashboard
8. Verify widget renders correctly

**Implementation approach:**
- Import { test } from './midscene.fixture'
- Use page.route() to mock OAuth provider endpoints (GitHub, Google)
- Use page.route() to mock SQL responses for data (D-06)
- Use page.route() to mock webview scrape responses (D-07)
- Use agent.aiAct(), agent.aiWaitFor(), agent.aiAssert() for AI-driven interactions

**Mock strategy (D-05, D-06, D-07):**
```typescript
test.beforeEach(async ({ page }) => {
  // Mock OAuth provider
  await page.route('**/github.com/login/oauth/**', async (route) => {
    return route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ access_token: 'test_token' }) });
  });

  // Mock SQL responses
  await page.route('**/api/sources*', async (route) => {
    return route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify([{ id: 'test-source', name: 'Test Source', status: 'success' }]) });
  });
});
```

The test should use AI to drive the browser naturally, not hardcoded selectors.
</action>
  <verify>
    <automated>cat /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react/tests/e2e/midscene/critical-path.spec.ts | grep -E "aiAct|aiWaitFor|aiAssert|test\.beforeEach.*route"</automated>
  </verify>
  <done>critical-path.spec.ts exists, implements full D-01 critical path with AI-driven browser automation and mock strategies</done>
</task>

</tasks>

<verification>
cd /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react && pnpm exec playwright test tests/e2e/midscene/critical-path.spec.ts --list 2>&1 | head -10
</verification>

<success_criteria>
- critical-path.spec.ts file exists with aiAct/aiWaitFor/aiAssert calls
- Test includes page.route() mocks for OAuth (D-05), SQL (D-06), webview (D-07)
- Test reads predefined YAML configs from config/examples/test-integrations/
- Test covers full critical path: create integration -> create source -> dashboard -> auth/form -> add widget -> render
</success_criteria>

<output>
After completion, create `.planning/phases/01-midscene-ai-harness/01-02-SUMMARY.md`
</output>
