---
phase: 08-ui-refactoring
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["ui-react/src/components/TopNav.tsx"]
autonomous: true
requirements: ["REQ-UI-REF-002"]
must_haves:
  truths:
    - "TopNav renders with a standard height of 64px (h-16)"
    - "Navigation items are icon-based and avoid text-heavy buttons"
    - "Action buttons use tooltips for text descriptions"
    - "Buttons exhibit classic contrast inversion on hover (150ms transition)"
    - "Interactive elements exhibit purple focus ring states"
  artifacts:
    - path: "ui-react/src/components/TopNav.tsx"
      provides: "Main navigation header"
  key_links:
    - from: "ui-react/src/components/TopNav.tsx"
      to: "ui-react/src/components/ui/tooltip.tsx"
      via: "Tooltip wrapping"
      pattern: "Tooltip(Provider|Trigger|Content)"
---

<objective>
Refactor core layout, navigation, and settings views.

Purpose: To transition the top navigation to a high-density, icon-driven format with tooltips, minimizing visual noise and reclaiming space for metrics.
Output: Refactored `TopNav.tsx`.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refactoring/08-CONTEXT.md
@.planning/phases/08-ui-refactoring/08-RESEARCH.md
</context>

<interfaces>
From ui-react/src/components/ui/tooltip.tsx:
```tsx
export const TooltipProvider: React.FC<TooltipProviderProps>;
export const Tooltip: React.FC<TooltipProps>;
export const TooltipTrigger: React.FC<TooltipTriggerProps>;
export const TooltipContent: React.FC<TooltipContentProps>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign TopNav Structure</name>
  <files>ui-react/src/components/TopNav.tsx</files>
  <action>
    Rewrite `TopNav.tsx` completely to enforce a standard height of 64px (`h-16`).
    - Remove text labels from the navigation links ("全部看板", "集成管理") and the refresh button ("全部刷新").
    - Replace them with purely icon-based triggers.
    - Wrap every icon trigger in a `Tooltip` (using `TooltipProvider`, `TooltipTrigger`, `TooltipContent` from `ui/tooltip`) to provide the text description.
  </action>
  <verify>
    <automated>grep -q "h-16" ui-react/src/components/TopNav.tsx && grep -q "Tooltip" ui-react/src/components/TopNav.tsx</automated>
  </verify>
  <done>TopNav is 64px tall and exclusively uses icon-based tooltips for actions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hover State Inversion & Accessibility</name>
  <files>ui-react/src/components/TopNav.tsx</files>
  <action>
    Apply the requested hover state styling to the icon buttons in `TopNav`.
    - Buttons should use a classic contrast inversion: default state with transparent/faint background, hover state instantly filling with dark color (or foreground) and text/icon inverting to background color.
    - Include a 150ms transition (e.g., `transition-colors duration-150`).
    - Explicitly implement focus states (`focus-visible:ring-2 focus-visible:ring-brand/50` or equivalent variables) on all interactive elements to ensure accessibility per CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q "duration-150" ui-react/src/components/TopNav.tsx && grep -q "focus-visible" ui-react/src/components/TopNav.tsx</automated>
  </verify>
  <done>Navigation buttons exhibit fast contrast inversion on hover and clear focus states for accessibility.</done>
</task>

</tasks>

<verification>
Run `npm run build` inside `ui-react` to ensure no syntax or typing errors in `TopNav.tsx`.
</verification>

<success_criteria>
`TopNav.tsx` is successfully modernized to an icon-driven, high-density component with proper accessibility.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refactoring/08-02-SUMMARY.md`
</output>