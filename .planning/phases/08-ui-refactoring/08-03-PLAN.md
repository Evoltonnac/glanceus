---
phase: 08-ui-refactoring
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["ui-react/src/components/BaseSourceCard.tsx", "ui-react/src/components/widgets/QuotaBar.tsx", "ui-react/src/App.tsx"]
autonomous: true
requirements: ["REQ-UI-REF-003"]
must_haves:
  truths:
    - "BaseSourceCard has a non-shifting absolute-positioned action bar on hover"
    - "BaseSourceCard applies soft-elevation shadow on hover"
    - "BaseSourceCard properly wires internal content to WidgetRenderer"
    - "Interactive elements exhibit purple focus ring states"
    - "QuotaBar displays metrics using tabular-nums"
    - "Long values in QuotaBar are truncated with an ellipsis and use tooltips"
    - "QuotaBar shows a brief background flash on data updates"
    - "App dashboard displays as a single column on narrow screens with 16px gap"
  artifacts:
    - path: "ui-react/src/components/BaseSourceCard.tsx"
      provides: "Card shell structure"
    - path: "ui-react/src/components/widgets/QuotaBar.tsx"
      provides: "High-density metric rendering"
    - path: "ui-react/src/App.tsx"
      provides: "Dashboard grid configuration"
  key_links:
    - from: "ui-react/src/components/BaseSourceCard.tsx"
      to: "ui-react/src/components/widgets/QuotaBar.tsx"
      via: "Widget rendering"
      pattern: "WidgetRenderer"
---

<objective>
Redesign dashboard widget presentation and source card interactions.

Purpose: To implement the core data presentation components using high-density formatting, tabular numbers, non-intrusive action peeks, animations, and accessible focus states.
Output: Refactored `BaseSourceCard.tsx`, `QuotaBar.tsx`, and responsive grid layout in `App.tsx`.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refactoring/08-CONTEXT.md
@.planning/phases/08-ui-refactoring/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign BaseSourceCard Overlay and Layout</name>
  <files>ui-react/src/components/BaseSourceCard.tsx</files>
  <action>
    Completely rewrite `BaseSourceCard.tsx` styles.
    - Remove old gradient logic (`qb-header-gradient-*`).
    - Set uniform padding (`p-4` or `p-5`) and remove inner nested borders.
    - Apply `hover:shadow-soft-elevation` for soft elevation on interaction (no `transform scale`).
    - Re-implement the hover action bar (edit, delete) using `absolute` positioning (`top-2 right-2` inside a `relative` container) and `opacity-0 group-hover:opacity-100` to prevent layout shifts. Ensure actions are icon-based with tooltips.
    - Explicitly implement focus states (e.g., `focus-visible:ring-2 focus-visible:ring-brand/50` or equivalent variables) on all interactive elements per CONTEXT.md.
    - Explicitly preserve the `WidgetRenderer` wiring within the card body to ensure child widget rendering doesn't break.
  </action>
  <verify>
    <automated>grep -q "group-hover:opacity-100" ui-react/src/components/BaseSourceCard.tsx &amp;&amp; grep -q "WidgetRenderer" ui-react/src/components/BaseSourceCard.tsx</automated>
  </verify>
  <done>BaseSourceCard is clean, flat, features a non-intrusive action peek with focus states, and preserves WidgetRenderer connection.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor QuotaBar with Tabular Nums & Animations</name>
  <files>ui-react/src/components/widgets/QuotaBar.tsx</files>
  <action>
    Modernize `QuotaBar.tsx` for high-density metrics.
    - Apply `tabular-nums` class to all numeric values.
    - For metric containers, use `min-w-0` and `truncate` to handle exceptionally long strings. If truncated, wrap the text in a `<Tooltip>`.
    - Change color conditions to use semantic colors (`bg-warning`, `bg-error`) without pulse animations when reaching limits.
    - Implement a data update animation (e.g., via a React useEffect or CSS keyframe on value change) that applies a brief background color flash to the numeric area when data updates.
  </action>
  <verify>
    <automated>grep -q "tabular-nums" ui-react/src/components/widgets/QuotaBar.tsx</automated>
  </verify>
  <done>Quota metrics align cleanly without horizontal shift, degrade gracefully on large values, and visually flash on update.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce Responsive Single-Column Grid</name>
  <files>ui-react/src/App.tsx</files>
  <action>
    Update grid layout settings in `App.tsx` (e.g. `GRID_MARGIN`, layout classes) to establish the `16px` gap.
    - Ensure that on narrow screens (e.g., mobile), the grid naturally falls back to a single column with adaptive (content-driven) heights, avoiding forced fixed proportions that clip content.
  </action>
  <verify>
    <automated>grep -E -q "gap-[0-9]+" ui-react/src/App.tsx &amp;&amp; grep -E -q "grid-cols-1|flex-col" ui-react/src/App.tsx</automated>
  </verify>
  <done>Dashboard layout properly switches to a single adaptive column on narrow viewports.</done>
</task>

</tasks>

<verification>
Run `npm run build` in `ui-react` to ensure all components compile without TypeScript or Tailwind issues.
</verification>

<success_criteria>
Dashboard cards and widgets adhere to the high-density minimalist design rules with robust data truncation, proper focus rings, and non-disruptive hover states.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refactoring/08-03-SUMMARY.md`
</output>