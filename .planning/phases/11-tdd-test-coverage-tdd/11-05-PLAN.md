---
phase: 11-tdd-test-coverage-tdd
plan: 05
type: tdd
wave: 2
depends_on:
  - 11-02
files_modified:
  - ui-react/src/pages/Integrations.test.tsx
  - ui-react/src/pages/dashboardLayout.ts
  - ui-react/src/pages/dashboardLayout.test.ts
  - ui-react/src/pages/Dashboard.tsx
autonomous: true
requirements:
  - "测试覆盖与 TDD 规范确立 (Test Coverage & TDD)"
must_haves:
  truths:
    - "Integrations page main flow is covered for load/select/edit/save/error behavior."
    - "Stored grid coordinates (`x/y/w/h`) are validated against overlap regressions."
    - "Dashboard layout persistence logic is testable via pure deterministic utilities."
  artifacts:
    - path: ui-react/src/pages/Integrations.test.tsx
      provides: "Main management-flow coverage for integrations."
    - path: ui-react/src/pages/dashboardLayout.ts
      provides: "Extracted deterministic layout helpers for overlap checks."
    - path: ui-react/src/pages/dashboardLayout.test.ts
      provides: "Non-overlap regression tests using stored layout coordinates."
  key_links:
    - from: ui-react/src/pages/Dashboard.tsx
      to: ui-react/src/pages/dashboardLayout.ts
      via: "Layout merge and persistence path used during grid change handling."
---

<objective>
Add frontend integration-flow and layout-safety coverage required by Phase 11 acceptance criteria.

Purpose: Prevent silent regression in integration management and persisted dashboard grid behavior.
Output: Behavioral tests for Integrations page chain plus deterministic non-overlap tests for stored layout coordinates.
</objective>

<context>
@.planning/phases/11-tdd-test-coverage-tdd/11-CONTEXT.md
@ui-react/src/pages/Integrations.tsx
@ui-react/src/pages/Dashboard.tsx
@ui-react/src/store/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Integrations Main-Flow Tests</name>
  <files>ui-react/src/pages/Integrations.test.tsx</files>
  <action>Using mocked API client responses, add tests covering the required chain: list load, file selection, content edit, save success path, and save error handling path.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/pages/Integrations.test.tsx</automated>
  </verify>
  <done>Integrations management page core flow is verified end-to-end at component level with deterministic mocks.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Deterministic Dashboard Layout Helpers</name>
  <files>ui-react/src/pages/dashboardLayout.ts, ui-react/src/pages/Dashboard.tsx</files>
  <action>Refactor grid node merge/validation logic from Dashboard into pure helper functions so layout behavior can be tested without GridStack runtime coupling.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/pages/dashboardLayout.test.ts</automated>
  </verify>
  <done>Dashboard layout mutation logic is reusable and independently testable.</done>
</task>

<task type="auto">
  <name>Task 3: Add Non-overlap Regression Tests for Stored x/y/w/h</name>
  <files>ui-react/src/pages/dashboardLayout.test.ts</files>
  <action>Write failing tests first for multi-card layout cases, asserting that persisted coordinates preserve ordering and do not produce overlap after merge/update operations.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/pages/dashboardLayout.test.ts</automated>
  </verify>
  <done>Phase 11 hard requirement for non-overlapping persisted `x/y/w/h` card layout is covered by regression tests.</done>
</task>

</tasks>

<verification>
Run `Integrations` and `dashboardLayout` tests together to confirm stateful page behavior and layout contracts pass in one deterministic suite.
</verification>

<success_criteria>
Frontend integration workflow and grid layout safety have explicit regression coverage aligned with Phase 11 acceptance rules.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tdd-test-coverage-tdd/11-05-SUMMARY.md`
</output>
