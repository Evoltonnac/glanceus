---
phase: 11-tdd-test-coverage-tdd
plan: 06
type: execute
wave: 3
depends_on:
  - 11-03
  - 11-04
  - 11-05
files_modified:
  - scripts/test_backend_core.sh
  - scripts/test_frontend_core.sh
  - scripts/test_impacted.sh
  - tests/smoke/test_phase11_smoke.py
  - .github/workflows/ci.yml
  - README.md
  - .planning/phases/11-tdd-test-coverage-tdd/11-UAT.md
autonomous: true
requirements:
  - "测试覆盖与 TDD 规范确立 (Test Coverage & TDD)"
must_haves:
  truths:
    - "Local quality gate commands exist for backend core tests, frontend core tests, and impacted-only test runs."
    - "CI blocks merges when backend core tests, frontend core tests, or frontend typecheck fail."
    - "Phase 11 includes a minimal smoke verification path without attempting full E2E matrix coverage."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Release-blocking CI quality gate wiring for Phase 11."
    - path: scripts/test_impacted.sh
      provides: "Local impacted-test workflow to keep feedback fast."
    - path: .planning/phases/11-tdd-test-coverage-tdd/11-UAT.md
      provides: "Minimal smoke checklist and expected outcomes."
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/test_backend_core.sh
      via: "CI job execution entry for backend blocking checks."
    - from: .github/workflows/ci.yml
      to: scripts/test_frontend_core.sh
      via: "CI job execution entry for frontend blocking checks."
---

<objective>
Wire Phase 11 quality gates into executable local scripts and CI blocking checks.

Purpose: Convert new test coverage into enforceable release criteria rather than optional checks.
Output: Local gate scripts, CI workflow, and minimal smoke/UAT artifacts.
</objective>

<context>
@.planning/phases/11-tdd-test-coverage-tdd/11-CONTEXT.md
@.planning/codebase/TESTING.md
@ui-react/package.json
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Local Gate and Impacted-Test Scripts</name>
  <files>scripts/test_backend_core.sh, scripts/test_frontend_core.sh, scripts/test_impacted.sh</files>
  <action>Create executable scripts for backend core suite, frontend core suite, and changed-file-driven impacted checks. Keep defaults fast while allowing explicit full-run override flags.</action>
  <verify>
    <automated>bash scripts/test_backend_core.sh && bash scripts/test_frontend_core.sh</automated>
  </verify>
  <done>Developers can run a fast local quality gate and an impacted-only variant before full CI.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI Blocking Workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>Implement CI jobs that run backend core tests, frontend core tests, and frontend typecheck; fail the pipeline on any gate failure to enforce Phase 11 release quality boundaries.</action>
  <verify>
    <automated>rg -n "test_backend_core|test_frontend_core|typecheck" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI enforces release-blocking checks defined in Phase 11 decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add Minimal Smoke and UAT Artifacts</name>
  <files>tests/smoke/test_phase11_smoke.py, .planning/phases/11-tdd-test-coverage-tdd/11-UAT.md, README.md</files>
  <action>Create one minimal smoke test path (non-matrix) and document its execution/expected result in `11-UAT.md`, then link the command matrix in README for teamless daily usage.</action>
  <verify>
    <automated>python -m pytest tests/smoke/test_phase11_smoke.py -q</automated>
  </verify>
  <done>Phase 11 has a reproducible minimal smoke path while explicitly deferring full E2E matrix to Phase 13.</done>
</task>

</tasks>

<verification>
Run local gate scripts and confirm CI workflow references the same command entry points to avoid environment drift.
</verification>

<success_criteria>
Phase 11 test coverage is enforceable by default through local scripts and CI blocking gates, with minimal smoke verification in place.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tdd-test-coverage-tdd/11-06-SUMMARY.md`
</output>
