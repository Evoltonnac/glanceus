---
phase: 11-tdd-test-coverage-tdd
plan: 04
type: tdd
wave: 2
depends_on:
  - 11-02
files_modified:
  - ui-react/src/components/widgets/WidgetRenderer.test.tsx
  - ui-react/src/components/ui/MetricCard.test.tsx
  - ui-react/src/components/ui/BentoWidget.test.tsx
  - ui-react/src/test/render.tsx
  - ui-react/src/components/widgets/WidgetRenderer.tsx
autonomous: true
requirements:
  - "测试覆盖与 TDD 规范确立 (Test Coverage & TDD)"
must_haves:
  truths:
    - "WidgetRenderer type dispatch and unknown fallback behavior are covered by explicit assertions."
    - "Row-span mapping and key prop-to-rendering contracts are regression protected."
    - "MetricCard and BentoWidget core display behavior is validated without snapshot-only tests."
  artifacts:
    - path: ui-react/src/components/widgets/WidgetRenderer.test.tsx
      provides: "Core widget dispatch and fallback behavior coverage."
    - path: ui-react/src/components/ui/MetricCard.test.tsx
      provides: "Metric presentation and trend/status behavior coverage."
    - path: ui-react/src/components/ui/BentoWidget.test.tsx
      provides: "Container variant/header rendering coverage."
  key_links:
    - from: ui-react/src/components/widgets/WidgetRenderer.tsx
      to: ui-react/src/components/widgets/WidgetRenderer.test.tsx
      via: "Type-specific branch outputs and row-span style contract."
---

<objective>
Deliver frontend component-level release-blocking tests for widget rendering contracts.

Purpose: Lock down core rendering behavior where UI breakage would silently degrade dashboard correctness.
Output: TDD-based tests for WidgetRenderer, MetricCard, and BentoWidget behavior contracts.
</objective>

<context>
@.planning/phases/11-tdd-test-coverage-tdd/11-CONTEXT.md
@ui-react/src/components/widgets/WidgetRenderer.tsx
@ui-react/src/components/ui/MetricCard.tsx
@ui-react/src/components/ui/BentoWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover WidgetRenderer Dispatch and Fallback Branches</name>
  <files>ui-react/src/components/widgets/WidgetRenderer.test.tsx, ui-react/src/components/widgets/WidgetRenderer.tsx</files>
  <action>Write failing tests for each widget type branch (`list`, `hero_metric`, `key_value_grid`, `progress_bar`) plus unknown-type fallback message; verify row-span default and custom override behavior before patching implementation details.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/components/widgets/WidgetRenderer.test.tsx</automated>
  </verify>
  <done>WidgetRenderer behavior contracts are explicit and guarded against regression for dispatch and fallback paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add MetricCard Behavioral Tests</name>
  <files>ui-react/src/components/ui/MetricCard.test.tsx</files>
  <action>Implement tests for title/value/description rendering, trend sign + value behavior, and status indicator rendering across key `statusColor` values.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/components/ui/MetricCard.test.tsx</automated>
  </verify>
  <done>MetricCard critical UI contracts (content + trend + status signal) are test-protected.</done>
</task>

<task type="auto">
  <name>Task 3: Add BentoWidget Variant and Header Tests</name>
  <files>ui-react/src/components/ui/BentoWidget.test.tsx, ui-react/src/test/render.tsx</files>
  <action>Create tests for variant class behavior and conditional header rendering (title/icon/subtitle/headerAction) with a shared render utility to keep assertions stable and readable.</action>
  <verify>
    <automated>cd ui-react && npm run test -- --run src/components/ui/BentoWidget.test.tsx</automated>
  </verify>
  <done>BentoWidget variant and header behavior is covered with reusable testing helpers.</done>
</task>

</tasks>

<verification>
Run all component-level tests together and confirm no snapshot-only assertions are used for critical rendering behavior.
</verification>

<success_criteria>
Frontend component contracts identified in Phase 11 are covered with deterministic behavior tests and ready for CI blocking usage.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tdd-test-coverage-tdd/11-04-SUMMARY.md`
</output>
