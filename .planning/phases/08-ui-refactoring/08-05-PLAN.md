---
phase: 08-ui-refactoring
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["ui-react/src/components/AddWidgetDialog.tsx", "ui-react/src/components/auth/FlowHandler.tsx", "ui-react/src/components/auth/OAuthCallback.tsx"]
autonomous: true
requirements: ["REQ-UI-REF-003"]
must_haves:
  truths:
    - "Dialogs are clean, flat, and utilize soft-elevation hover on interactive options"
    - "Forms and inputs inside dialogs use correct semantic tokens and focus rings"
  artifacts:
    - path: "ui-react/src/components/AddWidgetDialog.tsx"
      provides: "Widget creation dialog"
    - path: "ui-react/src/components/auth/FlowHandler.tsx"
      provides: "Auth flow UI"
---

<objective>
Refactor Dialogs and Flows.

Purpose: To bring all floating dialogs, forms, and interaction flows into full compliance with the new design system.
Output: Modernized `AddWidgetDialog.tsx`, `FlowHandler.tsx`, and `OAuthCallback.tsx`.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refactoring/08-CONTEXT.md
@.planning/phases/08-ui-refactoring/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor AddWidgetDialog</name>
  <files>ui-react/src/components/AddWidgetDialog.tsx</files>
  <action>
    - Clean up the modal layout.
    - Replace default list items with cards utilizing `hover:shadow-soft-elevation` and contrast inversion for selection.
    - Ensure high density and explicit focus rings.
  </action>
  <verify>
    <automated>grep -q "shadow-soft-elevation" ui-react/src/components/AddWidgetDialog.tsx</automated>
  </verify>
  <done>AddWidgetDialog uses interactive hover cards and standard focus states.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor FlowHandler & OAuthCallback</name>
  <files>ui-react/src/components/auth/FlowHandler.tsx</files>
  <action>
    - Modernize the layout of `FlowHandler.tsx` and `OAuthCallback.tsx`.
    - Ensure form inputs, status messages, and buttons inside these flows match the new standard (`bg-surface`, `focus-visible:ring-brand/50`, etc.).
  </action>
  <verify>
    <automated>grep -q "focus-visible" ui-react/src/components/auth/FlowHandler.tsx</automated>
  </verify>
  <done>Authentication and interactive flows align with the unified UI framework.</done>
</task>

</tasks>

<verification>
Run `npm run build` in `ui-react`.
</verification>

<success_criteria>
All dialogs and flows present a cohesive, accessible, and polished user experience consistent with the rest of the refactored dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refactoring/08-05-SUMMARY.md`
</output>
