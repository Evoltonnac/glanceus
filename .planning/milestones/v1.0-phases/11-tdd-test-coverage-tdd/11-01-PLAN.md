---
phase: 11-tdd-test-coverage-tdd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pytest.ini
  - tests/conftest.py
  - tests/factories/__init__.py
  - tests/factories/source_factory.py
  - docs/testing_tdd.md
  - README.md
autonomous: true
requirements:
  - "测试覆盖与 TDD 规范确立 (Test Coverage & TDD)"
must_haves:
  truths:
    - "Backend core modules can be validated with one deterministic command."
    - "Async test setup is standardized and reusable across executor/auth/api tests."
    - "Backend TDD rules are documented as explicit release-blocking workflow rules."
  artifacts:
    - path: requirements.txt
      provides: "Backend test dependencies (pytest stack)."
    - path: pytest.ini
      provides: "Repository-level backend test discovery and async mode defaults."
    - path: docs/testing_tdd.md
      provides: "Canonical TDD and quality-gate policy for Phase 11."
  key_links:
    - from: docs/testing_tdd.md
      to: pytest.ini
      via: "Documented commands must match runnable defaults."
---

<objective>
Establish the backend testing and TDD baseline for all Phase 11 execution work.

Purpose: Replace ad-hoc script testing with a repeatable, command-driven baseline before adding coverage.
Output: Pytest runtime baseline, shared test factories, and a documented backend TDD workflow.
</objective>

<context>
@.planning/phases/11-tdd-test-coverage-tdd/11-CONTEXT.md
@.planning/codebase/TESTING.md
@tests/verify_interactions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Backend Test Runtime</name>
  <files>requirements.txt, pytest.ini</files>
  <action>Add pytest-based dependencies (including asyncio support) and create `pytest.ini` with strict async test mode, test path defaults, and warning policy suitable for CI blocking checks.</action>
  <verify>
    <automated>python -m pytest tests -q</automated>
  </verify>
  <done>Backend tests can be discovered and executed through a single pytest command without custom script bootstrapping.</done>
</task>

<task type="auto">
  <name>Task 2: Create Reusable Backend Fixtures and Factories</name>
  <files>tests/conftest.py, tests/factories/__init__.py, tests/factories/source_factory.py</files>
  <action>Extract reusable executor/auth source builders and shared mocks from `tests/verify_interactions.py` into fixtures/factories to support API key, OAuth, cURL, and webview flow tests.</action>
  <verify>
    <automated>python -m pytest tests -q -k "factory or fixture or interaction"</automated>
  </verify>
  <done>New backend tests can build valid source configs and mocked runtime dependencies without duplicating setup code.</done>
</task>

<task type="auto">
  <name>Task 3: Publish Backend TDD Rules and Commands</name>
  <files>docs/testing_tdd.md, README.md</files>
  <action>Document mandatory red-green cycle scope for backend core modules, hotfix exception policy, and local/CI command matrix so Phase 11 execution has explicit quality gates.</action>
  <verify>
    <automated>rg -n "TDD|red|green|hotfix|pytest" docs/testing_tdd.md README.md</automated>
  </verify>
  <done>TDD and backend test gate rules are explicit, discoverable, and aligned with runnable commands.</done>
</task>

</tasks>

<verification>
Run the documented backend baseline command from a clean shell to confirm test discovery and async execution operate without manual setup.
</verification>

<success_criteria>
Backend test infra is no longer script-only; Phase 11 backend coverage plans can execute using shared fixtures and clear TDD policy.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tdd-test-coverage-tdd/11-01-SUMMARY.md`
</output>
