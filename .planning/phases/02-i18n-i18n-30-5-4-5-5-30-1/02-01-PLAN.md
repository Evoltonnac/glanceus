---
phase: 02-i18n-i18n-30-5-4-5-5-30-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/error_formatter.py
  - core/executor.py
  - core/api.py
  - tests/core/test_error_formatter.py
  - tests/api/test_reload_error_boundary.py
autonomous: true
requirements:
  - "P2-REQ-02"
must_haves:
  truths:
    - "Users see consistently structured runtime error messages across backend and UI surfaces."
    - "Error payloads are localization-ready and no longer rely on ad-hoc string formatting."
  artifacts:
    - path: "core/error_formatter.py"
      provides: "Canonical error envelope builder for runtime and API flows."
    - path: "core/executor.py"
      provides: "Flow/runtime error conversion to a stable summary/details shape."
  key_links:
    - from: "core/executor.py"
      to: "core/error_formatter.py"
      via: "normalize runtime exceptions into formatted envelope"
    - from: "core/api.py"
      to: "core/error_formatter.py"
      via: "serialize formatted errors for frontend consumers"
---

# Phase 2 Plan 01: Standardize Runtime Error Envelope

## Objective
- **What:** Introduce a backend-owned error formatting contract (`code/summary/details`) and apply it to runtime/API outputs.
- **Why:** Phase 2 requires a consistent error format as the foundation for i18n.
- **Output:** Reusable formatter module, executor integration, and regression tests for API-visible behavior.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-i18n-i18n-30-5-4-5-5-30-1/02-RESEARCH.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Add canonical backend error formatter and unit coverage</name>
  <files>core/error_formatter.py, core/executor.py, tests/core/test_error_formatter.py</files>
  <action>Create a dedicated formatter utility that maps runtime exceptions to a stable, localization-ready envelope (`code`, `summary`, `details`, optional `step_id`). Refactor executor error conversion to use this utility while preserving current control flow semantics.</action>
  <verify>
    <automated>pytest tests/core/test_error_formatter.py -q</automated>
  </verify>
  <done>Executor-generated failures consistently produce predictable code/summary/details output without ad-hoc string branching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align API-facing error serialization and regression tests</name>
  <files>core/api.py, tests/api/test_reload_error_boundary.py</files>
  <action>Ensure API responses that expose runtime failures preserve the new formatted structure and remain backward-safe for existing frontend consumers. Extend API regression tests to assert stable structure and fallback behavior.</action>
  <verify>
    <automated>pytest tests/api/test_reload_error_boundary.py -q</automated>
  </verify>
  <done>Frontend callers can rely on a uniform error payload contract across key failure paths.</done>
</task>

## Verification
- Trigger representative runtime failures and confirm `summary/details` are deterministic and human-readable.
- Validate API regression tests cover both structured and fallback error cases.

## Success Criteria
- [ ] A single canonical formatter owns runtime error presentation semantics.
- [ ] API consumers receive consistent localization-ready error envelopes.
