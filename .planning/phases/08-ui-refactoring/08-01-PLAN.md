---
phase: 08-ui-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["ui-react/src/index.css", "ui-react/tailwind.config.js"]
autonomous: true
requirements: ["REQ-UI-REF-001"]
must_haves:
  truths:
    - "Application renders with correct base background colors for both light and dark modes"
    - "Application visual style is flat and minimalist without legacy gradient headers"
    - "Interactive elements display a soft elevation shadow when hovered"
  artifacts:
    - path: "ui-react/src/index.css"
      provides: "CSS variables and theme configuration"
    - path: "ui-react/tailwind.config.js"
      provides: "Tailwind shadow extensions"
  key_links:
    - from: "ui-react/tailwind.config.js"
      to: "ui-react/src/index.css"
      via: "CSS variables"
      pattern: "var\\(--surface\\)"
---

<objective>
Establish unified design system and component library guidelines.

Purpose: To prepare the styling foundation (CSS variables and Tailwind tokens) for the upcoming component refactoring, ensuring "High-Density Minimalist" constraints and semantic colors are ready.
Output: Updated `index.css` and `tailwind.config.js`.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refactoring/08-CONTEXT.md
@.planning/phases/08-ui-refactoring/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Semantic CSS Variables</name>
  <files>ui-react/src/index.css</files>
  <action>
    Update `:root` and `.dark` blocks to define strict semantic variables based on `08-RESEARCH.md`.
    - Ensure `--background`, `--surface`, `--muted`, `--border`, and `--ring` are correctly mapped.
    - Set explicit semantic colors: `--brand` (Violet 600), `--warning` (Orange 500), `--success` (Emerald 500), `--error` (Red 500).
    - Remove all `qb-header-gradient-*` classes and the `qb-pulse-gradient` animation, as they are replaced by the new minimalist design.
  </action>
  <verify>
    <automated>grep -q "\-\-surface:" ui-react/src/index.css &amp;&amp; ! grep -q "qb-header-gradient" ui-react/src/index.css</automated>
  </verify>
  <done>CSS variables reflect true light/dark duality and gradient classes are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind Extensions</name>
  <files>ui-react/tailwind.config.js</files>
  <action>
    Extend the Tailwind theme to include the new "soft elevation" box shadow required for interactive card hover states.
    - Add a custom `boxShadow` named `soft-elevation` that provides a multi-layered, soft Z-axis effect.
    - Ensure semantic color variables are mapped properly if not already complete.
  </action>
  <verify>
    <automated>grep -q "soft-elevation" ui-react/tailwind.config.js</automated>
  </verify>
  <done>Tailwind config exposes `shadow-soft-elevation`.</done>
</task>

</tasks>

<verification>
Check that the React build runs successfully without missing class errors and `index.css` is valid.
</verification>

<success_criteria>
`index.css` and `tailwind.config.js` are updated, and the application compiles correctly with the new styling foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refactoring/08-01-SUMMARY.md`
</output>